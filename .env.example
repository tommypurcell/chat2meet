# Copy to `.env` and fill in. Never commit `.env`.

# Firebase Admin (server only). Full service account JSON as a single line.
# Example shape: {"type":"service_account","project_id":"…","private_key":"…",...}
FIREBASE_SERVICE_ACCOUNT=
